# Core web + queue. Stdlib does the rest.
flask>=3.0

# Real-model + real-sandbox path. Everything runs without these in local mode
# with the scripted backend, which is how tests and CI run.
anthropic>=0.40 ; extra == "live"
steel-sdk>=0.1 ; extra == "live"
playwright>=1.40 ; extra == "live"
python-dotenv>=1.0 ; extra == "live"

# The in-sandbox egress proxy. tripwire/localproxy.py is a stdlib stand-in used
# for local runs and CI, so mitmproxy is only needed inside a Steel Computer.
mitmproxy>=10 ; extra == "live"
